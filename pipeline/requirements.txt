# Authoritative runtime dependencies for the complete REGDOCS Atlas pipeline.
# Tested with Python 3.12.3; Python 3.10 or newer is required.
#
# Install once for Scout, Download, Azure analysis, Docling analysis,
# Normalize, Index, migrations, and recovery:
#   python -m pip install -r pipeline/requirements.txt

# Scout / Download
beautifulsoup4==4.15.0
httpx==0.28.1
lxml==6.1.1
tqdm==4.68.4

# Azure Content Understanding
azure-ai-contentunderstanding==1.1.0
azure-core==1.41.0
azure-identity==1.25.3
pypdf==6.14.2

# Local Docling analysis. Docling brings its ML/runtime dependencies, so this
# makes the unified environment heavier but removes the second dependency set.
docling>=2,<3

# Azure AI Search
azure-search-documents==12.0.0
