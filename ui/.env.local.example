# Azure AI Search queried by server-side Next.js route handlers.
# Local development: run `az login` and grant your identity Search Index Data Reader.
# Azure App Service: use a system-assigned managed identity with the same data-plane role.
AZURE_SEARCH_ENDPOINT=https://<service-name>.search.windows.net
AZURE_SEARCH_INDEX=regdocs-chunks
