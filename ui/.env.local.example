# REGDOCS Atlas server-side Azure AI Search configuration.
# Keep this file server-side. Never expose the key through NEXT_PUBLIC_* variables.
AZURE_SEARCH_ENDPOINT=https://<service-name>.search.windows.net
AZURE_SEARCH_INDEX=regdocs-chunks
AZURE_SEARCH_API_KEY=<query-key-or-admin-key>
