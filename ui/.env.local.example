# REGDOCS Atlas server-side Azure AI Search configuration.
AZURE_SEARCH_ENDPOINT=https://<service-name>.search.windows.net
AZURE_SEARCH_INDEX=regdocs-current
AZURE_SEARCH_RETRIEVAL_MODE=hybrid
AZURE_SEARCH_SEMANTIC_CONFIG=regdocs-semantic

# Stage 6 graph and chronology indexes.
AZURE_SEARCH_ENTITIES_INDEX=regdocs-entities
AZURE_SEARCH_RELATIONS_INDEX=regdocs-relations
AZURE_SEARCH_EVENTS_INDEX=regdocs-events

# Microsoft Foundry deployments used by grounded Ask and hybrid query vectors.
FOUNDRY_PROJECT_ENDPOINT=https://<resource>.services.ai.azure.com/api/projects/<project>
FOUNDRY_MODEL_DEPLOYMENT=<responses-model-deployment>
FOUNDRY_EMBEDDING_DEPLOYMENT=<embedding-deployment>
FOUNDRY_EMBEDDING_DIMENSIONS=1536

# Optional privacy-preserving salt used to derive Responses API safety identifiers.
# FOUNDRY_SAFETY_SALT=<long-random-secret>

# Optional for local development. Use a read-only query key, never an admin key.
# In Azure App Service, omit this setting and use the managed identity instead.
# Never expose the key through a NEXT_PUBLIC_* variable.
AZURE_SEARCH_API_KEY=<query-key>

# Optional local-only alternative to managed identity for Foundry.
# FOUNDRY_API_KEY=<foundry-key>

# Optional local Stage 6 fallback when Azure intelligence indexes are absent.
# REGDOCS_INTELLIGENCE_DIR=../workspace/6_enrich
